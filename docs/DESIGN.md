# JobTrack - Design Document

## 1. Goal
A personal job-application tracker. Each user manages only their own data.

## 2. Data model

### users
| column | type | notes |
|---|---|---|
| id | uuid (PK) | generated by DB |
| email | text, unique, not null | stored lowercase |
| password_hash | text, not null | bcrypt hash, never the raw password |
| full_name | text, not null | |
| created_at | timestamptz, not null | default now() |

### applications
| column | type | notes |
|---|---|---|
| id | uuid (PK) | |
| user_id | uuid (FK -> users.id, on delete cascade) | owner |
| company | text, not null | |
| role_title | text, not null | |
| status | enum application_status | wishlist, applied, phone_screen, interview, offer, rejected, withdrawn |
| source | text, nullable | linkedin, company_site, referral, other |
| job_url | text, nullable | |
| location | text, nullable | |
| notes | text, nullable | free text |
| applied_at | date, nullable | |
| created_at | timestamptz | |
| updated_at | timestamptz | |

Indexes: (user_id), (user_id, status).

### application_events
| column | type | notes |
|---|---|---|
| id | uuid (PK) | |
| application_id | uuid (FK -> applications.id, on delete cascade) | |
| type | text with CHECK | note, status_change, interview, follow_up |
| content | text, not null | |
| happened_at | timestamptz, not null | default now() |
| created_at | timestamptz | |

Index: (application_id, happened_at desc).

## 3. API contract
All routes are prefixed with /api. JSON in, JSON out.
Errors: { "error": { "code": "STRING_CODE", "message": "human readable", "details": [...] } }

### Auth
| method | path | body | response |
|---|---|---|---|
| POST | /auth/register | { email, password, fullName } | 201 { user } + sets cookie |
| POST | /auth/login | { email, password } | 200 { user } + sets cookie |
| POST | /auth/logout | - | 204, clears cookie |
| GET | /auth/me | - | 200 { user } or 401 |

### Applications (auth required)
| method | path | query / body | response |
|---|---|---|---|
| GET | /applications | ?status=&search=&page=1&limit=20&sort=created_at:desc | 200 { items, page, limit, total } |
| POST | /applications | { company, roleTitle, status?, source?, jobUrl?, location?, notes?, appliedAt? } | 201 { application } |
| GET | /applications/:id | - | 200 { application, events } |
| PATCH | /applications/:id | partial fields | 200 { application } |
| DELETE | /applications/:id | - | 204 |

### Events (auth required)
| method | path | body | response |
|---|---|---|---|
| POST | /applications/:id/events | { type, content, happenedAt? } | 201 { event } |
| DELETE | /applications/:id/events/:eventId | - | 204 |

### Stats (auth required)
| method | path | response |
|---|---|---|
| GET | /stats | 200 { byStatus: [{status, count}], perWeek: [{week, count}], conversionRate } |

### Health (public)
| method | path | response |
|---|---|---|
| GET | /health | 200 { status: "ok", db: "ok", uptime } or 503 |

## 4. Screens
- /login, /register
- / (dashboard): stat cards + chart of applications per week
- /applications: table with filters (status, search), pagination, "new" button
- /applications/new: form
- /applications/:id: details, edit, delete, timeline of events, add event

## 5. Non-functional
- Auth: JWT in HttpOnly, Secure, SameSite=Lax cookie. 7 days expiry.
- All list endpoints paginated. Max limit 100.
- Every request logged as JSON with a request id.
- Health endpoint used by Docker HEALTHCHECK and by the deploy smoke test.
